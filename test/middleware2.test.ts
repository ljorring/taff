import { Context } from '@azure/functions';
import * as middleware2 from '../src/middleware2'

describe('middleware2', () => {
  it('dumb test', async () => {
    // Arrange
    let kage = 
  });
});
